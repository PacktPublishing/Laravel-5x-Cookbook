language: php

php:
  - 5.6

before_script:
  - travis_retry composer self-update
  - travis_retry composer install --prefer-source --no-interaction --dev
  - cp .env.travis .env

script:
  - phpunit
  - vendor/bin/behat -ptravis -shome_ui
  - vendor/bin/behat -ptravis -shome_domain